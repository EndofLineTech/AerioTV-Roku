# Retired fullscreen star experiments

These 0.3.10 diagnostic sources are retained as investigation history. They are
not part of the application package or active test suite. The PO rejected inset
workarounds and pivoted away from fullscreen star after the tap/hold experiments
failed. Development now uses hold OK for player options.

See `docs/FULLSCREEN-OPTIONS-RESEARCH.md` at the repository root for findings.
